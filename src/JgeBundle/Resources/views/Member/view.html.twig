<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{% block title %}JGE Football {% endblock %}</title>

    {% block stylesheets %}
        {# On charge le CSS de bootstrap depuis le site directement #}
        <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    {% endblock %}
</head>

<body>
<div class="container">
    <div id="header" class="jumbotron">
        <h1>JGE Football</h1>
        <h2>Jeune Garde de l'Erdre · 2017 - 2018</h2>

    </div>

    <div class="row">
        <div id="menu" class="col-md-3">
            <h3>Membres</h3>


        </div>
        <div id="content" class="col-md-9">
            {% block body %}
                <h2>{{ member.firstname }} {{ member.lastname }}</h2>

                <div class="well">
                    <p>Licence n° : {{ member.licenceNum }}</p>
                    {% for role in member %}
                        <li>
                            <p>Rôle(s) au sein du club : {{ role.role }}</p>
                        </li>
                    {% endfor %}
                    <p>Né(e) le : {{ member.dateOfBirth |date("d/m/Y")}}</p>
                    <p>Adresse(s) mail :{{ member.email }}</p>
                    <p>Mot de passe : {{ member.password }}</p>
                    <p>Numéro(s) de tel. : {{ member.telNumber }}</p>
                    <p>Sexe : {{ member.sex }}</p>
                    <p>Pointure : {{ member.shoeSize }}</p>
                    <p>Taille (en cm) : {{ member.heigth }}</p>
                </div>

                <p>
                    <a href="{{ path('jge_member_list') }}" class="btn btn-default">
                        <i class="glyphicon glyphicon-chevron-left"></i>
                        Retour à la liste
                    </a>
                    <a href="{{ path('jge_member_update', {'id': member.id}) }}" class="btn btn-default">
                        <i class="glyphicon glyphicon-edit"></i>
                        Modifier les infos
                    </a>
                    <a href="{{ path('jge_member_delete', {'id': member.id}) }}" class="btn btn-danger">
                        <i class="glyphicon glyphicon-trash"></i>
                        Supprimer le membre
                    </a>
                </p>
            {% endblock %}
        </div>
    </div>

    <hr>

    <footer>
        <p>JEGE Football © {{ 'now'|date('Y') }}</p>
    </footer>
</div>

</body>
